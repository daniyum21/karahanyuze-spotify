name: Deploy to HostMonster

on:
  push:
    branches:
      - main
  workflow_dispatch:  # Allow manual trigger

# Cancel any in-progress deployments when a new one starts
concurrency:
  group: deploy-to-hostmonster
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        
      - name: 🐘 Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: mbstring, xml, pdo_mysql, gd, zip, curl, fileinfo, bcmath
          tools: composer:v2
          
      - name: 📦 Cache Composer dependencies
        uses: actions/cache@v3
        with:
          path: ~/.composer/cache
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-composer-
            
      - name: 📦 Install Composer dependencies
        run: |
          composer install --optimize-autoloader --no-dev --prefer-dist --no-interaction --no-progress
          
      - name: 📦 Cache NPM dependencies
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-
            
      - name: 📦 Install NPM dependencies
        run: |
          npm ci --prefer-offline --no-audit
          
      - name: 🔨 Build assets
        run: |
          npm run build
          echo "✅ Assets built"
          echo "📦 Build directory contents:"
          ls -lah public/build/ || echo "⚠️  Build directory not found!"
          echo "📦 Build manifest check:"
          test -f public/build/manifest.json && echo "✅ manifest.json exists" || echo "❌ manifest.json not found!"
          
      - name: 🔨 Create deployment package
        run: |
          echo "Creating deployment package..."
          mkdir -p deploy-package
          
          # Copy files excluding unnecessary ones
          rsync -av \
            --exclude='.git' \
            --exclude='node_modules' \
            --exclude='tests' \
            --exclude='storage/logs/*' \
            --exclude='storage/framework/cache/*' \
            --exclude='storage/framework/sessions/*' \
            --exclude='storage/framework/views/*' \
            --exclude='storage/app/Audios' \
            --exclude='storage/app/Pictures' \
            --exclude='storage/app/public' \
            --exclude='.env' \
            --exclude='.env.example' \
            --exclude='deploy-package' \
            --exclude='docker-compose.yml' \
            --exclude='.github' \
            --exclude='*.md' \
            --exclude='.gitignore' \
            --exclude='.gitattributes' \
            --exclude='.editorconfig' \
            --exclude='database export' \
            --exclude='new look' \
            --exclude='oldApp' \
            --exclude='fix-mysql.sh' \
            --exclude='import-database.sh' \
            . deploy-package/
          
          # Create tarball and show size
          tar -czf deploy-package.tar.gz -C deploy-package .
          echo "📦 Deployment package size:"
          ls -lh deploy-package.tar.gz
          echo "📊 Package contents size:"
          du -sh deploy-package/
          
      - name: 📤 Upload to HostMonster via SCP
        id: scp-upload
        uses: appleboy/scp-action@v0.1.7
        continue-on-error: true
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: ${{ secrets.SSH_PORT || '22' }}
          source: "deploy-package.tar.gz"
          target: "~/"
          timeout: 600s
          strip_components: 0
          overwrite: true
          debug: true
          use_insecure_cipher: true
          
      - name: ⏳ Wait 10 seconds before retry
        if: steps.scp-upload.outcome == 'failure'
        run: sleep 10
        
      - name: 🔄 Retry SCP upload (if failed)
        if: steps.scp-upload.outcome == 'failure'
        id: scp-retry
        uses: appleboy/scp-action@v0.1.7
        continue-on-error: true
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: ${{ secrets.SSH_PORT || '22' }}
          source: "deploy-package.tar.gz"
          target: "~/"
          timeout: 600s
          strip_components: 0
          overwrite: true
          debug: true
          use_insecure_cipher: true
          
      - name: ⚠️  SSH Connection Failed
        if: steps.scp-upload.outcome == 'failure' && steps.scp-retry.outcome == 'failure'
        run: |
          echo "❌ SSH connection failed after retries"
          echo ""
          echo "This is likely a server-side issue with HostMonster:"
          echo "  - SSH service might be down"
          echo "  - Firewall might be blocking connections"
          echo "  - Port might be incorrect"
          echo ""
          echo "Please check:"
          echo "  1. SSH is enabled in HostMonster cPanel"
          echo "  2. SSH port is correct (default: 22)"
          echo "  3. Firewall/security settings"
          echo ""
          echo "See MANUAL_DEPLOY.md for manual deployment instructions."
          exit 1
          
      - name: 🚀 Deploy on HostMonster
        id: ssh-deploy
        if: steps.scp-upload.outcome == 'success' || steps.scp-retry.outcome == 'success'
        uses: appleboy/ssh-action@v1.0.3
        continue-on-error: true
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: ${{ secrets.SSH_PORT || '22' }}
          timeout: 600s
          command_timeout: 600s
          debug: true
          use_insecure_cipher: true
          script: |
            set -e
            
            echo "🔧 Starting deployment process..."
            
            # Create private directory if it doesn't exist
            mkdir -p private/karahanyuze-spotify
            
            # Extract Laravel app to private directory
            echo "📂 Extracting Laravel app to private directory..."
            tar -xzf deploy-package.tar.gz -C private/karahanyuze-spotify
            
            # Copy public folder contents to public_html/iwacu (preserving other domain folders)
            echo "📂 Copying public files to public_html/iwacu (preserving other domains)..."
            mkdir -p public_html/iwacu
            if [ -d "private/karahanyuze-spotify/public" ]; then
              # Verify build directory exists before copying
              if [ -d "private/karahanyuze-spotify/public/build" ]; then
                echo "✅ Build directory exists in package"
                echo "   Build files: $(find private/karahanyuze-spotify/public/build -type f | wc -l)"
              else
                echo "⚠️  Build directory NOT found in package!"
                echo "   This may cause CSS/JS assets to fail loading"
              fi
              
              # Use rsync without --delete to preserve existing files
              # Important: Use trailing slash on source to copy contents, not the folder itself
              # And ensure destination path is explicit to avoid overwriting parent directories
              rsync -av \
                private/karahanyuze-spotify/public/ ~/public_html/iwacu/
              echo "✅ Public files copied to public_html/iwacu (other domains preserved)"
              
              # Verify build directory was copied
              if [ -d ~/public_html/iwacu/build ]; then
                echo "✅ Build directory exists in public_html/iwacu"
                echo "   Build files: $(find ~/public_html/iwacu/build -type f | wc -l)"
              else
                echo "❌ Build directory NOT found in public_html/iwacu after copy!"
              fi
              
              # IMPORTANT: Do NOT overwrite .htaccess during deployment
              # The .htaccess file is configured manually and should not be overwritten
              # If .htaccess is missing, run: bash fix-iwacu-htaccess-assets.sh
              echo "⚠️  Skipping .htaccess update (preserving existing configuration)"
              echo "   If .htaccess is missing or broken, run: bash fix-iwacu-htaccess-assets.sh"
              cd ~/public_html/iwacu
              if [ ! -f ".htaccess" ]; then
                echo "⚠️  Warning: .htaccess not found! Run fix-iwacu-htaccess-assets.sh after deployment"
              else
                echo "✅ .htaccess exists (preserved)"
              fi
            else
              echo "⚠️  Warning: public folder not found in package"
            fi
            
            # Don't modify public_html/.htaccess or public_html/index.php
            # They should remain unchanged for other domains
            echo "✅ Leaving public_html/.htaccess and public_html/index.php unchanged for other domains"
            
            # Update index.php in iwacu to point to karahanyuze-spotify
            echo "🔧 Updating index.php in iwacu to point to karahanyuze-spotify..."
            cd ~/public_html/iwacu
            if [ -f "index.php" ]; then
              # Backup original
              cp index.php index.php.backup
              
              # Update paths to point to private/karahanyuze-spotify directory
              sed -i "s|__DIR__.'/../vendor/autoload.php'|__DIR__.'/../../private/karahanyuze-spotify/vendor/autoload.php'|g" index.php
              sed -i "s|__DIR__.'/../bootstrap/app.php'|__DIR__.'/../../private/karahanyuze-spotify/bootstrap/app.php'|g" index.php
              
              echo "✅ iwacu/index.php updated to point to karahanyuze-spotify"
            else
              echo "⚠️  Warning: index.php not found in iwacu"
            fi
            
            # Don't modify public_html/index.php - it should stay as is for other domains
            echo "✅ Leaving public_html/index.php unchanged for other domains"
            
            # Set up environment
            cd ~/private/karahanyuze-spotify
            
            # Clear caches FIRST before checking .env (important for fresh config)
            echo "🧹 Clearing all caches before environment setup..."
            php artisan config:clear || true
            php artisan cache:clear || true
            php artisan view:clear || true
            php artisan route:clear || true
            # Remove cached config files (they may contain old APP_URL)
            rm -f bootstrap/cache/config.php 2>/dev/null || true
            rm -rf storage/framework/cache/* 2>/dev/null || true
            
            # Preserve existing .env file
            if [ -f .env ]; then
              echo "✅ Using existing .env file"
              
              # CRITICAL: Fix APP_URL if it contains file system paths
              # This MUST be fixed before any config caching
              if grep -q "^APP_URL=" .env; then
                APP_URL_VALUE=$(grep "^APP_URL=" .env | cut -d '=' -f2- | tr -d ' ' | tr -d '"' | tr -d "'")
                echo "📄 Current APP_URL: $APP_URL_VALUE"
                # Fix if it contains ANY file system paths or doesn't contain the domain
                if [[ "$APP_URL_VALUE" == *"/home"* ]] || [[ "$APP_URL_VALUE" == *"public_html"* ]] || [[ "$APP_URL_VALUE" == *"/"* && "$APP_URL_VALUE" != *"://"* ]] || [[ "$APP_URL_VALUE" != *"iwacu.org"* ]]; then
                  echo "❌ APP_URL is incorrect (contains path or wrong domain): $APP_URL_VALUE"
                  echo "🔧 Fixing APP_URL to https://iwacu.org..."
                  sed -i "s|^APP_URL=.*|APP_URL=https://iwacu.org|g" .env
                  echo "✅ APP_URL fixed to https://iwacu.org"
                  # Remove cached config to force regeneration
                  rm -f bootstrap/cache/config.php 2>/dev/null || true
                else
                  echo "✅ APP_URL looks correct: $APP_URL_VALUE"
                fi
              else
                echo "⚠️  APP_URL not set, adding..."
                echo "" >> .env
                echo "APP_URL=https://iwacu.org" >> .env
                echo "✅ APP_URL added"
              fi
              
              # Remove incorrect ASSET_URL if it exists
              if grep -q "^ASSET_URL=" .env; then
                ASSET_URL_VALUE=$(grep "^ASSET_URL=" .env | cut -d '=' -f2- | tr -d ' ' | tr -d '"' | tr -d "'")
                if [[ "$ASSET_URL_VALUE" == *"/home"* ]] || [[ "$ASSET_URL_VALUE" == *"public_html"* ]] || [[ "$ASSET_URL_VALUE" == *"/"* && "$ASSET_URL_VALUE" != *"://"* ]]; then
                  echo "❌ ASSET_URL is incorrect: $ASSET_URL_VALUE"
                  echo "🔧 Removing incorrect ASSET_URL..."
                  sed -i "/^ASSET_URL=/d" .env
                  echo "✅ ASSET_URL removed (will use APP_URL)"
                fi
              fi
              
              # Ensure DB_CONNECTION is set to mysql (not sqlite)
              if ! grep -q "^DB_CONNECTION=" .env; then
                echo "🔧 Setting DB_CONNECTION to mysql..."
                echo "" >> .env
                echo "# Database Connection" >> .env
                echo "DB_CONNECTION=mysql" >> .env
                echo "✅ DB_CONNECTION set to mysql"
              else
                DB_CONNECTION_VALUE=$(grep "^DB_CONNECTION=" .env | cut -d '=' -f2 | tr -d ' ')
                if [ "$DB_CONNECTION_VALUE" != "mysql" ]; then
                  echo "⚠️  DB_CONNECTION is set to $DB_CONNECTION_VALUE (should be mysql)"
                  echo "🔧 Updating DB_CONNECTION to mysql..."
                  sed -i "s|^DB_CONNECTION=.*|DB_CONNECTION=mysql|g" .env
                  echo "✅ DB_CONNECTION updated to mysql"
                else
                  echo "✅ DB_CONNECTION is correctly set to mysql"
                fi
              fi
              
              # Clear config cache again after .env changes
              echo "🧹 Clearing config cache after .env changes..."
              php artisan config:clear || true
              
              # Check if APP_KEY is missing or empty, if so generate it
              APP_KEY_VALUE=$(grep "^APP_KEY=" .env 2>/dev/null | cut -d '=' -f2- | tr -d ' ')
              if [ -z "$APP_KEY_VALUE" ] || [ "$APP_KEY_VALUE" = "" ]; then
                echo "🔑 Generating application key..."
                php artisan key:generate --force
                echo "✅ Application key generated"
              else
                echo "✅ APP_KEY already exists in .env"
              fi
              
              # Check if APP_CIPHER is missing or incorrect, set it to aes-256-gcm (default for Laravel 12)
              if ! grep -q "^APP_CIPHER=" .env; then
                echo "🔐 Setting APP_CIPHER to aes-256-gcm..."
                echo "" >> .env
                echo "# Application Cipher" >> .env
                echo "APP_CIPHER=aes-256-gcm" >> .env
                echo "✅ APP_CIPHER set"
              else
                # Check if cipher is supported
                APP_CIPHER_VALUE=$(grep "^APP_CIPHER=" .env | cut -d '=' -f2 | tr -d ' ')
                if [ "$APP_CIPHER_VALUE" != "aes-128-cbc" ] && [ "$APP_CIPHER_VALUE" != "aes-256-cbc" ] && [ "$APP_CIPHER_VALUE" != "aes-128-gcm" ] && [ "$APP_CIPHER_VALUE" != "aes-256-gcm" ]; then
                  echo "⚠️  APP_CIPHER is set to unsupported value: $APP_CIPHER_VALUE"
                  echo "🔐 Updating APP_CIPHER to aes-256-gcm..."
                  sed -i "s|^APP_CIPHER=.*|APP_CIPHER=aes-256-gcm|g" .env
                  echo "✅ APP_CIPHER updated to aes-256-gcm"
                else
                  echo "✅ APP_CIPHER is correctly set to $APP_CIPHER_VALUE"
                fi
              fi
            else
              if [ -f .env.example ]; then
                echo "📝 Copying .env.example to .env"
                cp .env.example .env
                echo "🔑 Generating application key..."
                php artisan key:generate --force
                echo "🔐 Setting APP_CIPHER to aes-256-gcm..."
                if ! grep -q "^APP_CIPHER=" .env; then
                  echo "" >> .env
                  echo "# Application Cipher" >> .env
                  echo "APP_CIPHER=aes-256-gcm" >> .env
                fi
                echo "⚠️  IMPORTANT: Please update .env file with your production settings!"
              else
                echo "⚠️  .env file not found! Please create one manually."
              fi
            fi
            
            # Create necessary directories
            echo "📁 Creating storage directories..."
            mkdir -p storage/logs
            mkdir -p storage/framework/cache/data
            mkdir -p storage/framework/sessions
            mkdir -p storage/framework/views
            mkdir -p storage/app/public
            mkdir -p storage/app/Audios
            mkdir -p storage/app/Pictures
            mkdir -p bootstrap/cache
            
            # Set permissions
            echo "🔐 Setting permissions..."
            chmod -R 755 storage
            chmod -R 755 bootstrap/cache
            chmod -R 775 storage/logs
            chmod -R 775 storage/framework
            
            # Clear all caches first (IMPORTANT: Must clear config cache before checking DB)
            echo "🧹 Clearing caches..."
            php artisan config:clear || true
            php artisan route:clear || true
            php artisan view:clear || true
            
            # Test database connection and run migrations (if .env exists)
            if [ -f .env ]; then
              echo "🔍 Testing database connection..."
              
              # Verify DB_CONNECTION is set correctly in .env
              if grep -q "^DB_CONNECTION=mysql" .env; then
                echo "✅ DB_CONNECTION is set to mysql in .env"
              else
                echo "⚠️  DB_CONNECTION is not set to mysql, fixing..."
                if grep -q "^DB_CONNECTION=" .env; then
                  sed -i "s|^DB_CONNECTION=.*|DB_CONNECTION=mysql|g" .env
                else
                  echo "DB_CONNECTION=mysql" >> .env
                fi
                echo "✅ DB_CONNECTION updated to mysql"
                # Clear config cache again after changing .env
                php artisan config:clear || true
              fi
              
              # Test database connection (skip table count check to save time)
              if php artisan db:show 2>&1 | grep -q "Connection:"; then
                echo "✅ Database connection successful"
              else
                echo "❌ Database connection failed!"
                echo "   Please check your .env database settings:"
                echo "   - DB_CONNECTION=mysql (should be set)"
                echo "   - DB_HOST, DB_PORT, DB_DATABASE"
                echo "   - DB_USERNAME, DB_PASSWORD"
                echo "   - Database server must be accessible from this server"
                echo ""
                echo "   Current DB_CONNECTION value:"
                grep "^DB_CONNECTION=" .env || echo "   (not set)"
              fi
              
              # Run migrations
              echo "🗄️  Running database migrations..."
              if php artisan migrate --force 2>&1; then
                echo "✅ Migrations completed successfully"
              else
                echo "⚠️  Migration failed or migrations already applied"
                echo "   This is normal if migrations are already up to date"
              fi
            else
              echo "⚠️  Skipping migrations - .env file not found"
              echo "   To run migrations manually:"
              echo "   1. Create/upload .env file to ~/private/karahanyuze-spotify/"
              echo "   2. Run: cd ~/private/karahanyuze-spotify && php artisan migrate --force"
            fi
            
            # Run optimizations
            echo "⚡ Running optimizations..."
            php artisan config:cache || echo "⚠️  Config cache failed"
            php artisan route:cache || echo "⚠️  Route cache failed"
            # Don't cache views - they change frequently and caching can cause issues
            php artisan view:clear || echo "⚠️  View clear failed"
            
            # Create symbolic link from public_html/iwacu/storage to private storage
            cd ~/public_html/iwacu
            if [ ! -L storage ] && [ ! -d storage ]; then
              echo "🔗 Creating storage symbolic link..."
              ln -s ~/private/karahanyuze-spotify/storage/app/public storage
              echo "✅ Storage link created"
            elif [ -L storage ]; then
              echo "✅ Storage link already exists"
            else
              echo "⚠️  Storage directory exists as regular folder, not creating link"
            fi
            
            # Final cache clear to ensure fresh views are served
            echo "🧹 Final cache clear..."
            php artisan view:clear || true
            php artisan cache:clear || true
            
            # Clean up
            rm -f ~/deploy-package.tar.gz
            
            echo "✅ Deployment completed successfully!"
            echo "📂 Laravel app: ~/private/karahanyuze-spotify/"
            echo "🌐 Public files: ~/public_html/iwacu/"
            echo ""
            echo "⚠️  IMPORTANT REMINDERS:"
            echo "   1. Ensure storage/app/Audios/ and storage/app/Pictures/ are uploaded separately"
            echo "   2. Verify .env file has correct production settings"
            echo "   3. Check file permissions (storage and bootstrap/cache should be writable)"
            echo "   4. Other domain folders (indirimbo, kinyarwanda) have been preserved"
            echo "   5. Clear browser cache (Cmd+Shift+R or Ctrl+Shift+R) to see new styles"
            
      - name: ⏳ Wait 10 seconds before SSH retry
        if: steps.ssh-deploy.outcome == 'failure'
        run: sleep 10
        
      - name: 🔄 Retry SSH deployment (if failed)
        if: steps.ssh-deploy.outcome == 'failure'
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: ${{ secrets.SSH_PORT || '22' }}
          timeout: 600s
          command_timeout: 600s
          debug: true
          use_insecure_cipher: true
          script: |
            set -e
            
            echo "🔧 Starting deployment process (Retry)..."
            
            # Create private directory if it doesn't exist
            mkdir -p private/karahanyuze-spotify
            
            # Check if package exists
            if [ ! -f ~/deploy-package.tar.gz ]; then
              echo "❌ Deployment package not found. Upload may have failed."
              exit 1
            fi
            
            # Extract Laravel app to private directory
            echo "📂 Extracting Laravel app to private directory..."
            tar -xzf deploy-package.tar.gz -C private/karahanyuze-spotify
            
            # Copy public folder contents to public_html/iwacu (preserving other domain folders)
            echo "📂 Copying public files to public_html/iwacu (preserving other domains)..."
            mkdir -p public_html/iwacu
            if [ -d "private/karahanyuze-spotify/public" ]; then
              # Verify build directory exists before copying
              if [ -d "private/karahanyuze-spotify/public/build" ]; then
                echo "✅ Build directory exists in package"
                echo "   Build files: $(find private/karahanyuze-spotify/public/build -type f | wc -l)"
              else
                echo "⚠️  Build directory NOT found in package!"
                echo "   This may cause CSS/JS assets to fail loading"
              fi
              
              # Use rsync without --delete to preserve existing files
              # Important: Use trailing slash on source to copy contents, not the folder itself
              # And ensure destination path is explicit to avoid overwriting parent directories
              rsync -av \
                private/karahanyuze-spotify/public/ ~/public_html/iwacu/
              echo "✅ Public files copied to public_html/iwacu (other domains preserved)"
              
              # Verify build directory was copied
              if [ -d ~/public_html/iwacu/build ]; then
                echo "✅ Build directory exists in public_html/iwacu"
                echo "   Build files: $(find ~/public_html/iwacu/build -type f | wc -l)"
              else
                echo "❌ Build directory NOT found in public_html/iwacu after copy!"
              fi
              
              # IMPORTANT: Do NOT overwrite .htaccess during deployment
              # The .htaccess file is configured manually and should not be overwritten
              # If .htaccess is missing, run: bash fix-iwacu-htaccess-assets.sh
              echo "⚠️  Skipping .htaccess update (preserving existing configuration)"
              echo "   If .htaccess is missing or broken, run: bash fix-iwacu-htaccess-assets.sh"
              cd ~/public_html/iwacu
              if [ ! -f ".htaccess" ]; then
                echo "⚠️  Warning: .htaccess not found! Run fix-iwacu-htaccess-assets.sh after deployment"
              else
                echo "✅ .htaccess exists (preserved)"
              fi
            else
              echo "⚠️  Warning: public folder not found in package"
            fi
            
            # Don't modify public_html/.htaccess or public_html/index.php
            # They should remain unchanged for other domains
            echo "✅ Leaving public_html/.htaccess and public_html/index.php unchanged for other domains"
            
            # Update index.php in iwacu to point to karahanyuze-spotify
            echo "🔧 Updating index.php in iwacu to point to karahanyuze-spotify..."
            cd ~/public_html/iwacu
            if [ -f "index.php" ]; then
              # Backup original
              cp index.php index.php.backup
              
              # Update paths to point to private/karahanyuze-spotify directory
              sed -i "s|__DIR__.'/../vendor/autoload.php'|__DIR__.'/../../private/karahanyuze-spotify/vendor/autoload.php'|g" index.php
              sed -i "s|__DIR__.'/../bootstrap/app.php'|__DIR__.'/../../private/karahanyuze-spotify/bootstrap/app.php'|g" index.php
              
              echo "✅ iwacu/index.php updated to point to karahanyuze-spotify"
            else
              echo "⚠️  Warning: index.php not found in iwacu"
            fi
            
            # Don't modify public_html/index.php - it should stay as is for other domains
            echo "✅ Leaving public_html/index.php unchanged for other domains"
            
            # Set up environment
            cd ~/private/karahanyuze-spotify
            
            # Preserve existing .env file
            if [ -f .env ]; then
              echo "✅ Using existing .env file"
              
              # Ensure DB_CONNECTION is set to mysql
              if ! grep -q "^DB_CONNECTION=mysql" .env; then
                if grep -q "^DB_CONNECTION=" .env; then
                  sed -i "s|^DB_CONNECTION=.*|DB_CONNECTION=mysql|g" .env
                else
                  echo "DB_CONNECTION=mysql" >> .env
                fi
              fi
            fi
            
            # Create necessary directories
            echo "📁 Creating storage directories..."
            mkdir -p storage/logs storage/framework/cache/data storage/framework/sessions storage/framework/views storage/app/public storage/app/Audios storage/app/Pictures bootstrap/cache
            
            # Set permissions
            echo "🔐 Setting permissions..."
            chmod -R 755 storage bootstrap/cache
            chmod -R 775 storage/logs storage/framework
            
            # Clear caches
            echo "🧹 Clearing caches..."
            php artisan config:clear || true
            php artisan route:clear || true
            php artisan view:clear || true
            
            # Run migrations if .env exists (with error handling)
            if [ -f .env ]; then
              echo "🗄️  Running database migrations..."
              php artisan migrate --force 2>&1 | head -20 || echo "⚠️  Migration failed or already applied"
            fi
            
            # Run optimizations
            echo "⚡ Running optimizations..."
            php artisan config:cache || echo "⚠️  Config cache failed"
            php artisan route:cache || echo "⚠️  Route cache failed"
            # Don't cache views - they change frequently and caching can cause issues
            php artisan view:clear || echo "⚠️  View clear failed"
            
            # Create storage link
            cd ~/public_html/iwacu
            if [ ! -L storage ] && [ ! -d storage ]; then
              ln -s ~/private/karahanyuze-spotify/storage/app/public storage
            fi
            
            # Clean up
            rm -f ~/deploy-package.tar.gz
            
            echo "✅ Deployment completed successfully (Retry)!"
            
      - name: ✅ Check deployment status
        run: |
          if [ "${{ steps.ssh-deploy.outcome }}" == "success" ] || [ "${{ steps.ssh-deploy.outcome }}" == "failure" ]; then
            echo "✅ Deployment completed (may have succeeded even if step shows failure)"
            echo "🌐 Your application should be live!"
            echo ""
            echo "Note: SSH connection issues at the end don't always mean deployment failed."
            echo "Check your website to verify it's working."
          else
            echo "⚠️  Deployment status unclear"
          fi
          
      - name: 🧹 Cleanup
        if: always()
        run: |
          rm -rf deploy-package
          rm -f deploy-package.tar.gz

